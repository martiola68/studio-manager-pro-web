import { NextResponse } from "next/server";
import { createClient } from "@supabase/supabase-js";
async function context(request: Request){const token=String(request.headers.get("authorization")||"").replace(/^Bearer\s+/i,"");if(!token)throw new Error("UNAUTHORIZED");const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("CONFIG");const db=createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}});const{data:auth,error}=await db.auth.getUser(token);if(error||!auth.user?.email)throw new Error("UNAUTHORIZED");const{data:user}=await db.from("tbutenti").select("studio_id,tipo_utente,attivo").ilike("email",auth.user.email.toLowerCase()).maybeSingle();if(!user?.studio_id||!user.attivo||user.tipo_utente!=="Admin")throw new Error("FORBIDDEN");return{db,studioId:String(user.studio_id)}}
export async function GET(request:Request){try{const{db,studioId}=await context(request);let deletionAvailable=true,studio:any=null;const enhanced=await db.from("tbstudio").select("ragione_sociale,licenze_bypass,stato_cancellazione,data_richiesta_cancellazione,data_cancellazione_programmata,data_cessazione_abbonamento").eq("id",studioId).maybeSingle();if(enhanced.error){deletionAvailable=false;const fallback=await db.from("tbstudio").select("ragione_sociale,licenze_bypass").eq("id",studioId).maybeSingle();if(fallback.error)throw new Error("STUDIO_QUERY");studio=fallback.data}else studio=enhanced.data;if(!studio||studio.licenze_bypass)throw new Error("NOT_FOUND");const{data:l,error:le}=await db.from("tbsoftware_licenze").select("id,piano,stato,stripe_status,data_prossimo_pagamento,stripe_customer_id,stripe_subscription_id,stripe_current_period_end,data_scadenza,rinnovo_automatico,stripe_cancel_at_period_end").eq("studio_id",studioId).order("created_at",{ascending:false}).limit(1).maybeSingle();if(le||!l)throw new Error("NOT_FOUND");const ss=String(l.stripe_status||"").toLowerCase(),react=["canceled","unpaid","incomplete_expired","paused"].includes(ss)||l.stato==="sospeso",ds=deletionAvailable?studio.stato_cancellazione||null:null,scheduled=ds==="programmata"||ds==="richiesta";return NextResponse.json({ragione_sociale:studio.ragione_sociale,piano:l.piano||"—",stato:l.stato||"—",stripe_status:l.stripe_status,prossimo_pagamento:l.data_prossimo_pagamento||l.stripe_current_period_end||null,importo:null,valuta:"EUR",contract_end:l.data_scadenza||null,automatic_renewal:l.rinnovo_automatico!==false,cancellation_scheduled:l.rinnovo_automatico===false||!!l.stripe_cancel_at_period_end,can_manage_payment:!!l.stripe_customer_id&&!react,can_reactivate:!!l.stripe_customer_id&&!!l.stripe_subscription_id&&react,deletion_status:ds,deletion_requested_at:deletionAvailable?studio.data_richiesta_cancellazione||null:null,deletion_scheduled_at:deletionAvailable?studio.data_cancellazione_programmata||null:null,service_ended_at:deletionAvailable?studio.data_cessazione_abbonamento||null:null,can_request_deletion:deletionAvailable&&!scheduled,can_cancel_deletion:deletionAvailable&&scheduled,deletion_feature_ready:deletionAvailable})}catch(error:any){console.error("abbonamento/stato:",error);if(error?.message==="UNAUTHORIZED")return NextResponse.json({error:"Sessione scaduta o non valida"},{status:401});if(error?.message==="FORBIDDEN")return NextResponse.json({error:"Utente non autorizzato alla gestione dello studio"},{status:403});if(error?.message==="NOT_FOUND")return NextResponse.json({error:"Nessun abbonamento associato allo studio"},{status:404});return NextResponse.json({error:"Errore durante il caricamento dell’abbonamento"},{status:500})}}
